# Site URL (used for canonical URLs, sitemap, OG images)
NEXT_PUBLIC_SITE_URL=https://offertpro.se

# SMTP (for sending quotes/invoices via email)
# Leave empty to use console logging instead of real emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
